1. create database for dev and test
	a. storefront-backend-dev
	b. storefront-backend-test
2. connect to database using pool.
	a. install dontenv
	b. create an .env file to store enviromental variables

3. install migration db-migrate globally and install pg
	a. db-migration create <table> --sql-file
	b. run test migration to see if the datbase has connected succesfully

4. create tables for, orders product and user. and assign them with the neccessary relationship

5. Create crud action for all tables.
	a. tables can be represented using type = {...}
6. enable CORS on every endpoint.


7. hashing passwords using BCRYPT and salt
	a. store the salt round and the salt varibale in the .env variable
8. Use jwt to transfer data from the api to the front end.

9. Don't forget ot test all the endpoints
